# Server Configuration
server.port=${PORT:8085}
internal.secret=${INTERNAL_SECRET:CheiaMeaSecretaFoarteSigura_DS2025}

spring.application.name=monitoring-service

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_IP:localhost}:${DB_PORT:5432}/${DB_DBNAME:monitoring-db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# RabbitMQ Configuration
spring.rabbitmq.host=${RABBITMQ_IP:rabbitmq}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

# Monitaring Queue Configuration
monitoring.queue.name=${MONITORING_QUEUE_NAME:monitoring_queue_0}
monitoring.queue.name=monitoring_queue